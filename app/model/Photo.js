Ext.define('demo.model.Photo', {
    extend: 'Ext.data.Model',
    config: {
        fields: [
            'title',
            'farm',
            'server',
            'id',
            'secret'
        ]
    }
});
